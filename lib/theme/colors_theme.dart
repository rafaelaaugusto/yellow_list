import 'package:flutter/material.dart';

const primary = Color(0xFF000000);
const onPrimary = Color(0xFFFFFFFF);
const secondary = Color(0xFFFCCF4E);
const background = Color(0xFFF5F5F5);
const onBackground = Color(0xFFB5B5B5);

const colorSchemeLight = ColorScheme.light(
  primary: primary,
  onPrimary: onPrimary,
  secondary: secondary,
  background: background,
  onBackground: onBackground,
);
